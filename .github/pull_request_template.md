## Summary

(What changed? Why?)

## Checklist

- [ ] Unit tests pass (`python -m unittest discover -s tests -p 'test_*.py'`)
- [ ] No secrets in logs/output
- [ ] If behavior/user interface changed, docs updated (docs/** / README.md)
- [ ] Safe defaults preserved (no destructive actions by default)
